<?xml version="1.0" encoding="UTF-8"?>
<Configuration monitorInterval="<%= @monitorInterval -%>">
    <Appenders>
    </Appenders>

    <Loggers>
        <Root level="<%= @rootLevel -%>">
        </Root>
    </Loggers>
</Configuration>
